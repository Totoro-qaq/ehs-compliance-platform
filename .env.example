# ============================================================
# EHS Compliance API - 环境变量模板
# 用法：复制为 .env 后按需修改；带「必填」标注的字段必须填写。
#       .env 已在 .gitignore 中，禁止提交真实密钥。
# ============================================================

# --- 应用基础 ---
APP_NAME=EHS Compliance API
APP_ENV=dev
APP_DEBUG=true

# --- MySQL（必填）---
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=changeme
MYSQL_DB=ehs_system

# --- 引导管理员 ---
# 仅在该用户名不存在时由 init_db 创建；如需改密请用 scripts/upsert_bootstrap_admin.py
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=ChangeMe_Strong_Password

# --- Redis / Celery ---
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/1

# --- 上传 / 日志 ---
UPLOAD_DIR=./uploads
# 单文件上限（字节），默认 50MB
MAX_UPLOAD_BYTES=52428800
# 软删除文件保留天数，超期由 Beat 清理
UPLOAD_RETENTION_DAYS=7

LOG_DIR=./logs
LOG_FILE=ehs_api.log
LOG_WORKER_FILE=ehs_worker.log
LOG_LEVEL=INFO

# --- 安全 ---
# JWT 签名密钥；生产环境务必使用足够长的随机字符串
JWT_SECRET=please-change-me-to-a-long-random-string
JWT_EXPIRE_MINUTES=1440

# 管理员接口密钥（请求头 X-Admin-Key）；留空则 /api/v1/admin/* 返回 503
ADMIN_API_KEY=

# CORS 源，逗号分隔；* 表示任意（与 Cookie 鉴权不能同时使用浏览器 credentials）
CORS_ORIGINS=*

# --- Dify 工作流（必填）---
# 在 Dify 控制台「应用 -> API 访问」获取，仅工作流应用 Key 可用
DIFY_API_KEY=app-replace-with-your-dify-api-key
# 云版：https://api.dify.ai/v1   自建示例：http://localhost/v1
DIFY_BASE_URL=https://api.dify.ai/v1
# 与工作流「结束」节点输出变量名一致（默认 result）
DIFY_WORKFLOW_RESULT_KEY=result
# 与工作流「开始」中文本变量名一致（默认 document_text）
DIFY_WORKFLOW_INPUT_TEXT_KEY=document_text

# --- 出站 HTTP（可选）---
# HTTP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...
