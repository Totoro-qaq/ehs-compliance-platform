# 开发与测试依赖（不进生产镜像）
#
# 本地完整安装：
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# CI 中可以单独安装本文件，再叠加运行时依赖（按需剔除 PaddleOCR 等重型包）。
# 故本文件不再级联 -r requirements.txt，便于复用。
#
# 版本策略：仅设下限（>=），不设上限。
# Dependabot 在收到安全告警时需要"无上限的范围 + 实际安装版本"才能正确开
# 升级 PR；写死 <X.0 会让安全升级被卡在上限处。

pytest>=8.0
ruff>=0.8
