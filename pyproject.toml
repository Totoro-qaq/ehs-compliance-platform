[tool.pyright]
include = ["app", "main.py"]
pythonVersion = "3.13"
typeCheckingMode = "basic"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "single"
